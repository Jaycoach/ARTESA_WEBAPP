module.exports = {
    // Genera source maps para desarrollo
    sourceMap: true,
    // Mantiene los nombres de clase originales (no los minimiza)
    outputStyle: 'expanded',
    // Define la ruta relativa para los source maps
    sourceMapRoot: '/',
  };